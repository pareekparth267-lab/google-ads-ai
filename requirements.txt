# AdsForge AI — Requirements
# pip install -r requirements.txt

fastapi
uvicorn[standard]
httpx
pydantic
python-multipart

# Optional: if you want Google Ads official SDK (not required - app uses REST API)
# google-ads